{% extends 'base.html.twig' %}

{% block title %}Audience{% endblock %}

{% block body %}
    <h1>Audience</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ audience.id }}</td>
            </tr>
            <tr>
                <th>DateAudience</th>
                <td>{{ audience.dateAudience ? audience.dateAudience|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>AvisAudience</th>
                <td>{{ audience.avisAudience }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ audience.commentaire }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ audience.date ? audience.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_audience_index') }}">back to list</a>

    <a href="{{ path('app_audience_edit', {'id': audience.id}) }}">edit</a>

    {{ include('audience/_delete_form.html.twig') }}
{% endblock %}
