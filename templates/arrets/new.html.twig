{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-10 offset-lg-1">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title mb-0"> Arret  du dossier {{ dossier.referenceDossier }}  - {{ dossier.intituleObjet }}</h4>
                </div><!-- end card header -->

                <div class="card-body">
                    {{ include('arrets/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}


