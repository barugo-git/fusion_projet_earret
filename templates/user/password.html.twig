{% extends 'base.html.twig' %}

   {% block body%}

       <div class="row">
       <div class="col-lg-12">
       <div class="card">
       <div class="card-header">
           <h4 class="card-title mb-0"> Reinitialisation du compte de {{ user.fullName}}  </h4>
       </div><!-- end card header -->

       <div class="card-body">

           {{ form_start(form) }}
           <div class="row">

               <div class="col-md-6">{{ form_row(form.newPassword) }}</div>
               <div class="col-md-6">  {{ form_row(form.confirmPassword) }}</div>

               <div class="hstack gap-2 justify-content-center">
                   <button type="submit" class="btn btn-danger"  href="{{ path('app_admin_user') }}">Fermer</button>
                   <button class="btn btn-success" type="submit">Reinitialiser mot de passe</button>
{#                   <button type="submit" class="btn btn-success" id="add-btn" href="{{ path('reset_password_by_admin',{'id':user.id}) }}">Reinitialiser mot de passe</button>#}
                   {#                                   <button type="button" class="btn btn-success" id="edit-btn">Update</button>#}
               </div>
           </div>
           {{ form_end(form) }}

       </div>

   {% endblock %}