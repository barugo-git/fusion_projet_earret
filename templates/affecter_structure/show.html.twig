{% extends 'base.html.twig' %}

{% block title %}AffecterStructure{% endblock %}

{% block body %}
    <h1>AffecterStructure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ affecter_structure.id }}</td>
            </tr>
            <tr>
                <th>DateAffection</th>
                <td>{{ affecter_structure.dateAffection ? affecter_structure.dateAffection|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ affecter_structure.motif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_affecter_structure_index') }}">back to list</a>

    <a href="{{ path('app_affecter_structure_edit', {'id': affecter_structure.id}) }}">edit</a>

    {{ include('affecter_structure/_delete_form.html.twig') }}
{% endblock %}
