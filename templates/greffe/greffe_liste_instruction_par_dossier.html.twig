{% extends 'base.html.twig' %}


{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title mb-0">
                        Liste des mesures d'instructions ({{ dossier.referenceDossier }})<br> <br>
                        {% include 'partials/_intitule_dossier.html.twig' %}
                    </h4>
                </div><!-- end card header -->

                <div class="card-body">


                    <div class="data_table mt-3 mb-1">
                        {#                        {% if mesures|length > 0 %} #}
                        <table class="table align-middle table-nowrap">
                            <thead class="table-light">
                            <tr>
                                <th class="sort"></th>

                                <th class="sort" data-sort="customer_name">Mesures</th>
                                <th class="sort" data-sort="email">Parties</th>
                                <th class="sort" data-sort="phone">Délai</th>
                                <th class="sort" data-sort="date">Etat</th>
                                {#                                <th class="sort" data-sort="date">Décompte</th> #}
                                <th class="sort" data-sort="action">Action</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for mesure in mesures %}
                                <tr>
                                    <td>{{ loop.index }}</td>

                                    <td class="customer_name">{{ mesure.instruction.libelle }}</td>
                                    <td class="customer_name">{{ mesure.partiesConcernes }}</td>
                                    <td class="email"> Jours</td>

                                    <td class="email">
                                        {% if mesure.reponses %}
                                            Débutée
                                            {% if mesure.reponses.dateNotification %}
                                                <br> et notifie le  <br>
                                                {{ mesure.reponses.dateNotification | date('d-m-Y') }}
                                            {% else %}
                                                <br> non notifée a la partie concernée
                                            {% endif %}
                                        {% else %}
                                            Non débutée
                                        {% endif %}
                                    </td>
                                    {#                                    <td></td> #}
                                    <td>
                                        <div class="dropdown">
                                            <button class="btn btn-soft-secondary btn-sm dropdown" type="button"
                                                    data-bs-toggle="dropdown" aria-expanded="false">
                                                <i class="ri-more-fill"></i>
                                            </button>
                                            <ul class="dropdown-menu dropdown-menu-end">
                                                <li>
                                                    <a class="dropdown-item" href="{{ path('greffier_rapporteur_mesures_instructions_reponse_show',{'id':mesure.id}) }}">
                                                        <i class="ri-eye-fill align-bottom me-2 text-muted"></i>
                                                        Details de la mesure d'instruction
                                                    </a>
                                                </li>
                                                {% if is_granted('ROLE_GREFFIER') or is_granted('ROLE_GREFFIER_EN_CHEF') %}
                                                {% if mesure.reponses %}
                                                    <li>
                                                        <a class="dropdown-item" href="{{ path('greffier_rapporteur_mesures_instructions_reponse_edit',{'id':mesure.reponses.id}) }}">
                                                            <i class="ri-pencil-fill align-bottom me-2 text-muted"></i>
                                                            Completer ma reponse
                                                        </a>
                                                    </li>
                                                {% else %}
                                                <li>
                                                    <a class="dropdown-item"
                                                       href="{{ path('greffier_rapporteur_mesures_instructions_reponse',{'id':mesure.id}) }}">
                                                        <i class="ri-edit-2-fill align-bottom me-2 text-muted"></i>
                                                        Repondre</a>
                                                </li>

                                                {% endif %}
                                                {% endif %}
                                                <li class="dropdown-divider"> </li>
                                            </ul>
                                        </div>
{#                                        <div class="dropdown">#}
{#                                            <a href="#" role="button" id="dropdownMenuLink1"#}
{#                                               data-bs-toggle="dropdown" aria-expanded="false">#}
{#                                                <i class="ri-more-2-fill"></i>#}
{#                                            </a>#}

{#                                            <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink1">#}
{#                                                <li><a class="dropdown-item"#}
{#                                                       href="{{ path('greffier_rapporteur_mesures_instructions_reponse_show',{'id':mesure.id}) }}">Détails#}
{#                                                        de la mesure d'instruction</a></li>#}
{#                                                <li>#}
{#                                                    {% if is_granted('ROLE_GREFFIER') or is_granted('ROLE_GREFFIER_EN_CHEF') %}#}
{#                                                        {% if mesure.reponses %}#}
{#                                                            <a class="dropdown-item"#}
{#                                                               href="{{ path('greffier_rapporteur_mesures_instructions_reponse_edit',{'id':mesure.reponses.id}) }}">Completer#}
{#                                                                ma réponse</a>#}

{#                                                        {% else %}#}

{#                                                            <a class="dropdown-item"#}
{#                                                               href="{{ path('greffier_rapporteur_mesures_instructions_reponse',{'id':mesure.id}) }}">Repondre</a>#}

{#                                                        {% endif %}#}
{#                                                    {% endif %}#}

{#                                                </li>#}

{#                                            </ul>#}
{#                                        </div>#}

                                    </td>


                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                        {#                        {% else %} #}
                        {#                            <div class="noresult"> #}
                        {#                                <div class="text-center"> #}
                        {#                                    <lord-icon src="https://cdn.lordicon.com/msoeawqm.json" trigger="loop" #}
                        {#                                               colors="primary:#121331,secondary:#08a88a" #}
                        {#                                               style="width:75px;height:75px"> #}
                        {#                                    </lord-icon> #}
                        {#                                    <h5 class="mt-2">Aucune donnée pour l'instant</h5> #}
                        {#                                    #}{#                                       <p class="text-muted mb-0">We've searched more than 150+ Orders We did not find any #}
                        {#                                    #}{#                                           orders for you search.</p> #}
                        {#                                </div> #}
                        {#                            </div> #}
                        {#                        {% endif %} #}
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
