{% extends 'base.html.twig' %}


{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title mb-0"><br>
                       {% include 'partials/_intitule_dossier.html.twig' %}</h4>
                    Mesures d'instructions :
                </div><!-- end card header -->

                <div class="card-body">


                    <div class="data_table mt-3 mb-1">
                        {#                        {% if mesures|length > 0 %}#}
                        <table class="table align-middle table-nowrap">
                            <thead class="table-light">
                            <tr>
                                <th class="sort"></th>

                                <th class="sort" data-sort="customer_name">Mesures</th>
                                <th class="sort" data-sort="email">Parties</th>
                                <th class="sort" data-sort="date">Etat</th>
{#                                <th class="sort" data-sort="date">Décompte</th>#}

                            </tr>
                            </thead>
                            <tbody>
                            {% for mesure in mesures %}
                                <tr>
                                    <td>{{ loop.index }}</td>

                                    <td class="customer_name">{{ mesure.instruction }}</td>
                                    <td class="customer_name">{{ mesure.partiesConcernes }}</td>


                                    <td class="email">
                                        {% if mesure.reponses %}
                                            Débutée
                                            {% if mesure.reponses.dateNotification %}
                                                <br> et notifie le {{ mesure.reponses.dateNotification | date('d-m-Y') }}
                                            {% else %}
                                                <br> non notifée a la partie concernée
                                            {% endif %}
                                        {% else %}
                                            Non debutée
                                        {% endif %}
                                    </td>
{#                                    <td></td>#}




                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                        {#                        {% else %}#}
                        {#                            <div class="noresult">#}
                        {#                                <div class="text-center">#}
                        {#                                    <lord-icon src="https://cdn.lordicon.com/msoeawqm.json" trigger="loop"#}
                        {#                                               colors="primary:#121331,secondary:#08a88a"#}
                        {#                                               style="width:75px;height:75px">#}
                        {#                                    </lord-icon>#}
                        {#                                    <h5 class="mt-2">Aucune donnée pour l'instant</h5>#}
                        {#                                    #}{#                                       <p class="text-muted mb-0">We've searched more than 150+ Orders We did not find any#}
                        {#                                    #}{#                                           orders for you search.</p>#}
                        {#                                </div>#}
                        {#                            </div>#}
                        {#                        {% endif %}#}
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
